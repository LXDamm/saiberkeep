{
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc-watch --onSuccess \"node --es-module-specifier-resolution=node ./dist/app.js\"",
    "start": "echo \"Use yarn watch instead, or build with yarn build and then use node on ./dist/\""
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "mongoose": "^6.1.7",
    "typescript": "^4.5.5"
  }
}
